{
	"name": "nazare",
	"version": "0.13.3",
	"description": "Opinionated Shopify Liquid theme and component registry built for speed.",
	"bin": {
		"nazare": "./bin/nazare.js"
	},
	"scripts": {
		"test": "npm run test:unit",
		"test:unit": "vitest run test/features/**/unit.test.js test/features/**/runtime.test.js --testTimeout=15000",
		"test:feature": "vitest run",
		"test:features": "vitest run test/features/**/*.test.js --testTimeout=30000",
		"test:e2e": "NAZARE_RUN_SLOW_TESTS=1 vitest run test/e2e/*.test.js --testTimeout=300000",
		"test:all": "npm run test:features && npm run test:e2e",
		"test:build": "vitest run test/e2e/theme-build.test.js",
		"test:build:slow": "npm run test:e2e -- test/e2e/theme-build.test.js"
	},
	"license": "UNLICENSED",
	"private": true,
	"devDependencies": {
		"vitest": "^4.1.7"
	}
}
