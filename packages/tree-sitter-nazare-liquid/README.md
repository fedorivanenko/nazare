# @nazare/tree-sitter-nazare-liquid

Nazare extension of vendored `@nazare/tree-sitter-liquid` grammar. Adds explicit
CST nodes for component, import, props, blocks, Nazare render, script, and bound
stylesheet syntax.

Regenerate checked-in parser sources with `pnpm generate` after editing
`grammar.js`.
