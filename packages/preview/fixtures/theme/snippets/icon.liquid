{%- comment -%}
  A helper with no story file, on purpose. It renders nothing worth looking at
  on its own, so it stays out of the sidebar — but product-card renders it, so
  it has to stay in scope. Every theme has a hundred of these.
{%- endcomment -%}
<span class="icon icon--{{ name }}" aria-hidden="true">&#9733;</span>
