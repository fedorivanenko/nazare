node_modules/
dist/
.release/
.nazare-out/
*.tsbuildinfo
.claude/settings.local.json
.claude/

notes/

# Example theme build output + reconciliation baselines (regenerated by
# `nazare build`; the e2e test builds a temp copy, so these are never needed
# in git).
examples/theme/theme/
examples/theme/nazare.schema-lock.json
examples/theme/nazare.locales-base.json
examples/theme/nazare.migrations-applied.json
